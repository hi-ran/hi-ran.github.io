// const titleMain = document.querySelector('.title-main');
// const titleText = "신용평가 모형 추출 및 시각화";
// let index = 0;
// let isAnimated = false;

// function animateTitle() {
//     if (index < titleText.length) {
//       titleMain.textContent += titleText.charAt(index);
//       index++;
//       setTimeout(animateTitle, 100);
//     } else {
//       isAnimated = true;
//     }
//   }

//   window.addEventListener('scroll', () => {
//     const elementPosition = titleMain.getBoundingClientRect().top;
//     const screenPosition = window.innerHeight / 1.5; // 화면의 2/3 위치에서 나타나도록 설정

//     if (elementPosition < screenPosition && !isAnimated) {
//       animateTitle();
//     }
//   });
